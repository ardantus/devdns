# Daftar domain yang di-BLOCK (jawaban 0.0.0.0)
# Tambah manual: address=/domain.com/0.0.0.0
# Atau pakai script: ./scripts/add-block.sh domain.com
# Setelah edit, restart: docker compose restart

# Contoh (bisa dihapus):
# address=/ads.example.com/0.0.0.0
